spring:
  application:
    name: st-rest-server

  datasource:
    url: jdbc:mariadb://localhost:3306/st_rest
    username: jeong
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true